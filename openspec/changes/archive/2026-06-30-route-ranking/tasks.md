## 1. CSS

- [x] 1.1 Add `.rank-number` base styles (Arial, bold, flex alignment)
- [x] 1.2 Add `.route[data-rank="1"] .rank-number` Canyon Amber + larger size styles
- [x] 1.3 Add `.rank-header` flex row wrapper styles (wraps rank number + h2)

## 2. HTML

- [x] 2.1 Add `data-rank="1"` to Yellowstone `.route` div, wrap h2 in `.rank-header` flex row with `<span class="rank-number">#1</span>`
- [x] 2.2 Add `data-rank="2"` to El Capitan `.route` div, same flex row treatment with `#2`
- [x] 2.3 Add `data-rank="3"` to Bright Angel `.route` div, same flex row treatment with `#3`
- [x] 2.4 Confirm card order in HTML: Yellowstone → El Capitan → Bright Angel

## 3. Verify

- [x] 3.1 Run full test suite — all 3 test files must pass
